# Course content stays out of git by owner's choice (public repo):
# restore it per dl-exam-agent/README.md via scripts/restore_content.sh
dl-exam-agent/materials/
dl-exam-agent/index/
# ...except the site config, which is code-not-content and needed for replication
!dl-exam-agent/index/SITE_CONFIG.json
# Same policy for the statistics tutor (stats-exam-agent/README.md):
stats-exam-agent/materials/
stats-exam-agent/index/
# ...except the site config, which is code-not-content and needed for replication
!stats-exam-agent/index/SITE_CONFIG.json

# Python bytecode
__pycache__/
# Same policy for the reinforcement-learning tutor (rl-exam-agent/README.md):
rl-exam-agent/materials/
rl-exam-agent/index/
# ...except the site config, which is code-not-content and needed for replication
!rl-exam-agent/index/SITE_CONFIG.json
# Same policy for the Hebrew reinforcement-learning tutor (rl-he-exam-agent):
rl-he-exam-agent/materials/
rl-he-exam-agent/index/
!rl-he-exam-agent/index/SITE_CONFIG.json
